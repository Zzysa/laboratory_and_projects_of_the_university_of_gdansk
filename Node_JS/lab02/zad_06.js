const cat = { name: 'Filemon', age: 6 }

cat.description = `Cat has name ${cat.name} and has ${cat.age} years old. `

console.log(cat.description)

cat.breed = "birman"
cat.color = "white"

cat.description = `Cat has name ${cat.name} and has ${cat.age} years old. His breed is ${cat.breed} and his color is ${cat.color}`

console.log(cat.description)
